name: Sub1

on:
  workflow_call:
    secret:
      MY_SECRET:
        required: true

jobs:
  j1:
    runs-on: ubuntu-latest
    steps:
      - run: echo $S; echo $PWD; echo $PATH
        env:
          S: ${{ secrets.MY_SECRET }}

